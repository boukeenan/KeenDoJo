<Window x:Class="KeenDoJo.Dialogs.KeenPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:popup="clr-namespace:KeenDoJo.Dialogs"
        mc:Ignorable="d"
        Title="ToolTip Demo"
        Height="200"
        Width="250">

    <Grid Margin="10">

        <Button Width="100" Height="25" Click="Show_Click">Show Popup</Button>

        <Popup Name="MyPopup"
               Placement="Mouse"
               HorizontalOffset="-100"
               VerticalOffset="-100"
               AllowsTransparency="True">

            <Grid>
                <!--<Ellipse Width="200" Height="200" Fill="Aquamarine"/>
                <Button Click="Hide_Click" Margin="80">Hide</Button>-->
                <Grid x:Name="PopupGrid"
                      Background="AliceBlue" 
                      
                      Width="{Binding Width, RelativeSource={RelativeSource AncestorType=popup:KeenPopup}}"
                      Height="{Binding Height, RelativeSource={RelativeSource AncestorType=popup:KeenPopup}}">
                    <Label x:Name="MessageLabel" Content="message" />
                </Grid>
            </Grid>

        </Popup>
    </Grid>
</Window>
